# Read a line of input from the user
line = input("Enter something: ")

# Split the line into tokens (whitespace-separated by default)
tokens = line.split()

# Print each token
for token in tokens:
    print(token)
