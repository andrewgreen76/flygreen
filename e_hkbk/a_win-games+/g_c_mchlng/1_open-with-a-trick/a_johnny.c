#include <stdio.h>

int main()
{
  printf("\n");
  printf("Actually, this program is supposed to generate a file \"Here's Johnny!\" on your desktop with its name expanding character by character \n");
  printf("at every equal fraction of a second, with every completion of the spelling followed by renaming the file back to \n");
  printf("the initial letter of the hardcoded string \"Here's Johnny!\". And this goes on in an infinite loop. :) \n");
  printf("\n");

  return 0; 
}
