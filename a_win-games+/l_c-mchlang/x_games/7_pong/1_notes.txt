
===================================================================================
GAME LOGIC : 
===================================================================================

init :
 . srand(time(NULL));
 . bool asked_to_exit = false , newgame_reason = true;
   . try 1x "unsigned char" for 8x flags (init'd in main() ) - instead of wasting a byte on every bool. 


setup :
 . //

_
