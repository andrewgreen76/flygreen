# nums = [2,7,11,15], target = 9

import sys

instring = ' '.join(sys.argv[1:])    # CUT OFF THE FILENAME -> RENDER SYS.ARGV.TEXTTOKENS AS ONE STRING
numarr = re.findall(r'\d+', inarr)   # NOW DETECT ALL NUMBERS
